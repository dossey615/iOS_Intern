# Uncomment the next line to define a global platform for your project
  platform :ios, '11.3'

target 'iOS_Intern' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'TwitterCore'
  pod 'TwitterKit'

  # Pods for iOS_Intern

  target 'iOS_InternTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS_InternUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
